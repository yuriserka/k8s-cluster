{{- if .Values.secretEnv }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dev.fullname" . }}-secret
  labels:
    {{- include "dev.labels" . | nindent 4 }}
type: Opaque
stringData: {{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
